spring:
  application:
    name: limits-service
  cloud:
    config:
        uri: http://localhost:8888/spring-cloud-config-server/
  profiles:
    active: dev

server:
  port: 8080
  servlet.context-path: /limits-service