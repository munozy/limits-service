spring:
  application:
    name: limits-service
  cloud:
    config:
        uri: http://localhost:8888/
    bus.enabled: true
  profiles:
    active: dev

management.endpoints.web.exposure.include: "*"

server:
  port: 8080